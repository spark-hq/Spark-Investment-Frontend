# ===================================
# Spark Investment Frontend - Environment Configuration Example
# ===================================
# Copy this file to .env and configure for your environment

# API Configuration
# Backend API base URL
VITE_API_BASE_URL=http://localhost:5000/api

# Mock Mode Configuration
# General mock mode - affects investments, market data, AI, trading, etc.
# Set to 'true' to use mock data for non-deployed features
# Set to 'false' to connect to real backend for all features
VITE_MOCK_MODE=true

# Authentication Mock Mode - SEPARATE control for auth endpoints only
# Set to 'false' to use real backend for signup/login (recommended when backend auth is ready)
# Set to 'true' to use mock authentication
VITE_AUTH_MOCK_MODE=false

# Portfolio Mock Mode - SEPARATE control for portfolio endpoints only
# Set to 'false' to use real backend for portfolio APIs (even if backend has mock data)
# Set to 'true' to use frontend mock data for portfolio
VITE_PORTFOLIO_MOCK_MODE=false

# WebSocket Configuration (optional)
VITE_WEBSOCKET_URL=ws://localhost:5000

# Debug Mode - Enable detailed API logging
VITE_DEBUG_MODE=false
